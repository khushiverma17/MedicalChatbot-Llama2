from setuptools import find_packages, setup

setup(
    name = 'Medical ChatBot',
    version= '0.0.0',
    author= 'Pluto',
    author_email = 'khushiprojectid@gmail.com',
    packages=find_packages(), # when it will get constructor file __  __ then then folder is considered as local package
    install_requires=[]
)